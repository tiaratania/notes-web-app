@section ScriptSection {
    <script>

        @{ 
// TODO Lesson12 Task 2-2 Add code to perform ajax call to "/api/Exercise" to upload the form fmPicture
// Refer to 6P Part 1 slides
        }
        function uploadPicture() {
            var form = $("#fmPicture").get(0); // retrieve the HTMLFormElement object
            var fileinput = $("#picture").get(0); // retrieve the HTMLInputElement object
            var formdata = new FormData(form);

            // insert your code here
            $.ajax({
                url: "/api/Exercise",
                type: 'POST',
                data: formdata,
                success: function (data) {
                    if (data == 0) {
                        alert("Picture upload failed!")
                    }
                    else {
                        $("#imgEx2").attr("src", "/Exercise2/" + data)
                    }
                },
                contentType: false,
                processData: false
            });

        }

        $(document).ready(function () {
            $("#btnUploadPic").click(uploadPicture);
        });
    </script>
}
<form enctype="multipart/form-data" id="fmPicture" class="form-horizontal ">
    <div class="form-group">
        <h3 class="control-label.text-left col-offset-2 col-10">Exercise 2: Upload picture through web api</h3>
    </div>

    <div class="form-group">
        <label class="control-label col-2" for="picture">Picture:</label>
        <div class="col-10">
            <input type="file" class="form-control" id="picture" name="picture" accept=".jpg,.png">
        </div>
    </div>
    <div class="form-group">
        <div class="col-offset-2 col-10">
            <button id="btnUploadPic" type="button" class="btn btn-default">Submit</button>
        </div>
    </div>
</form>

<div class="col-offset-2 col-10">
    <img id="imgEx2" src="" alt="No picture uploaded" />
</div>

